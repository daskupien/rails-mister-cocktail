<h1><%= @cocktail.name %></h1>

<div id='cocktail-div'>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <%= dose.description %>
      <%= dose.ingredient.name %>
      <%# TODO get delete running %>
      <%= link_to "🗑", dose_path(dose), method: :delete %>
    </li>
  <% end %>
</div>
